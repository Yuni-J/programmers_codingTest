/* 조건에 맞는 사원 정보 조회하기 */

SELECT 
    SUM(HG.SCORE) AS SCORE	
    , HE.EMP_NO	
    , HE.EMP_NAME	
    , HE.POSITION	
    , HE.EMAIL
FROM HR_EMPLOYEES AS HE
JOIN HR_GRADE AS HG
    ON HE.EMP_NO = HG.EMP_NO
GROUP BY EMP_NO
ORDER BY SCORE DESC
LIMIT 1;