/* FrontEnd 개발자 찾기 */

SELECT DISTINCT
    ID
    , EMAIL
    , FIRST_NAME
    , LAST_NAME
FROM DEVELOPERS AS D
JOIN SKILLCODES AS S
    ON (D.SKILL_CODE & S.CODE) = S.CODE
WHERE S.CATEGORY = 'Front End'
ORDER BY ID;
